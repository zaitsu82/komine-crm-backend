# ===========================================
# Render Production Environment Variables
# ===========================================

# Database Configuration
# PgBouncer経由の接続（通常のクエリ用 — ポート6543）
# Supabase Dashboard > Settings > Database > Connection string > URI
# [YOUR-PASSWORD] を実際のパスワードに置き換えてください
DATABASE_URL="postgresql://postgres.[project-ref]:[YOUR-PASSWORD]@aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=10&pool_timeout=20"

# Direct URL（マイグレーション用 — PgBouncerを経由しない直接接続、ポート5432）
DIRECT_URL="postgresql://postgres.[project-ref]:[YOUR-PASSWORD]@db.[project-ref].supabase.co:5432/postgres"

# Server
PORT=4000
NODE_ENV=production

# CORS Configuration
# Vercel のデプロイ URL を設定してください
# 例: https://komine-frontend-crm-v2.vercel.app
ALLOWED_ORIGINS="https://your-vercel-app.vercel.app"

# Supabase Configuration
# Supabase Dashboard > Settings > API から取得
SUPABASE_URL="https://lsimjediamgktqvyaoqc.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key-here"

# Sentry Configuration (Optional)
# 本番環境ではサンプリングレートを下げることを推奨
# SENTRY_DSN="https://your-key@your-org.ingest.sentry.io/your-project-id"
# SENTRY_ENVIRONMENT="production"
# SENTRY_TRACES_SAMPLE_RATE="0.2"
